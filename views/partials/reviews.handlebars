<div class="row mt-4">
  <div class="col-md-6">

    <h3 class="reviews-text"></h3>
    {{#if reviews}}
    <h3>Tech Dawg's Blog Logs</h3>
   <div class = "gd-reviews"> 


      {{#each reviews as |review|}}
        <div class="card card-body mb-2">
          <h4>{{review.title}}</h4>
          <p>{{review.review}}</p>
        </div>
      {{!-- {{> delete}} --}}
      {{/each}}

    {{/if}}
    <div class="reviews">
    <h3>Dump your blog log below {{> poop}}</h3>


   </div>


    {{#if logged_in}}
    

    <form class="form new-review-form" id ="new-review-form">
      <div class="form-group">
        <label for="review-name">Post Title:</label>
        <input
          class="form-input"
          type="text"
          id="review-name"
          name="review-name"
        />
      </div>
      <div class="form-group">
        <label  for="review-desc">Post Description:</label>
        <textarea
          class="form-input"
          id="review-desc"
          name="review-desc"
        ></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>

    </div>

    {{/if}}

  </div>
</div>